import { FunctionComponent, PropsWithChildren } from "react";
import styles from "./Header.module.scss";


type Props = PropsWithChildren<{
}>


const LessonHeader: FunctionComponent<Props> = (props) => {
	return (
		<header>
		</header>
	);
};

export default LessonHeader;